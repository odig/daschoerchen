{% extends 'partials/base.html.twig' %}
{% set blog_image = page.media.images[page.header.hero_image] ?: page.media.images|first %}

{% set collection = page.collection() %}
{% set blog = page.find(header_var('blog_url')|defined(theme_var('blog-page'))) %}
{% set show_breadcrumbs = header_var('show_breadcrumbs', [page, blog])|defined(true) %}
{% set show_links = page.header.directorylisting.links|defined(false) %}
{% set dir_except = page.header.directorylisting.exclude_additional|defined(['ytlinks']) %}
{% set dir_include = page.header.directorylisting.include_additional|defined(['/choerchen-intern/choerchennoten']) %}
{% set route = page.header.directorylisting.include_additional|defined(['/choerchen-intern/choerchennoten'])|first %}


{% block body %}
	<section id="body-wrapper" class="section blog-listing">
		<section class="container {{ grid_size }}">

			{% if show_breadcrumbs and config.plugins.breadcrumbs.enabled %}
				{% include 'partials/breadcrumbs.html.twig' %}
			{% endif %}
			<div class="center">

				{% if page.title == "Gemeindeliederliste" %}
					<span class="h2">
						<a href="{{ base_url }}/choerchen-intern/gemeindelieder{{ uri.params() }}">Gemeindelieder</a>
					</span>

				{% else %}
					<span class="h2">
						<a href="{{ base_url }}/choerchen-intern/choerchennoten{{ uri.params() }}">Chörchennoten</a>
					</span>
				{% endif %}
				<sub>MP3, PDF und mehr</sub>

			</div>

			{{ page.summary|raw }}

			{% if page.title == "Gemeindeliederliste" %}
				{% set settings = {
					'exclude_main': true,
					'exclude_modular': true,
					'links': show_links,
					'route': '/choerchen-intern/gemeindelieder',
					'include_additional': dir_include,
					'exclude_additional': [ 'Archiv', 'archiv', 'Ytlinks', 'ytlinks' ],
					'order.by':'title',
					'order.dir':'asc',
					'level':3
				} %}
			{% else %}
				{% set settings = {
					'exclude_main': true,
					'exclude_modular': true,
					'links': show_links,
					'route': '/choerchen-intern/choerchennoten',
					'include_additional': ['/choerchen-intern/choerchennoten'],
					'exclude_additional': [ 'Archiv', 'archiv', 'Ytlinks', 'ytlinks' ],
					'order.by':'title',
					'order.dir':'asc',
					'level':3
				} %}

			{% endif %}

			{{ directorylisting(settings)|raw }}
		</section>
	</section>
	<script>// Bricklayer
// var bricklayer = new Bricklayer(document.querySelector('.bricklayer'))
</script>{% endblock %}
